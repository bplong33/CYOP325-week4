[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bad_code"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = [
  "fastapi[standard] >= 0.115.0"
]

[project.scripts]
vuln_app = "app:run"
