[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "path_traversal_mitigated"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = [
  "fastapi[standard] >= 0.115.0"
]

[project.scripts]
mitigated_app = "app:run"
